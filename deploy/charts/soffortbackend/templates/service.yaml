apiVersion: v1
kind: Service
metadata:
  name: {{ include "soffortbackend.fullname" . }}
  labels:
    {{- include "soffortbackend.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "soffortbackend.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP

